<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/button_ripple_color">
    
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="28dp" />
            <solid android:color="@color/white" />
        </shape>
    </item>
    
    <item>
        <selector>
            <!-- 按下状态 -->
            <item android:state_pressed="true">
                <shape android:shape="rectangle">
                    <corners android:radius="28dp" />
                    <gradient
                        android:startColor="@color/button_primary_gradient_end"
                        android:endColor="@color/button_primary_gradient_start"
                        android:angle="45" />
                </shape>
            </item>
            
            <!-- 禁用状态 -->
            <item android:state_enabled="false">
                <shape android:shape="rectangle">
                    <corners android:radius="28dp" />
                    <solid android:color="@color/button_disabled_background" />
                    <stroke android:width="1dp" android:color="@color/button_disabled_stroke" />
                </shape>
            </item>
            
            <!-- 普通状态 -->
            <item>
                <shape android:shape="rectangle">
                    <corners android:radius="28dp" />
                    <gradient
                        android:startColor="@color/button_primary_gradient_start"
                        android:endColor="@color/button_primary_gradient_end"
                        android:angle="45" />
                </shape>
            </item>
        </selector>
    </item>
    
</ripple>